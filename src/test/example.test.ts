//import { expect } from 'chai';

//describe('My Uber App', () => {
    //it('should display the Uber logo', async () => {
        // Use the accessibility ID to locate the Uber logo
       // const logoElement = await $(`~Uber logo`);

        // Wait for the element to be displayed, with a timeout of 10 seconds
      //  await logoElement.waitForDisplayed({ timeout: 10000 });

        // Check if the element is displayed
       // const isDisplayed = await logoElement.isDisplayed();
       // expect(isDisplayed).to.be.true;

        // Optionally, verify the content description (if needed)
       // const contentDesc = await logoElement.getAttribute('content-desc');
      //  expect(contentDesc).to.equal('Uber logo');
      
  //  });
//});
